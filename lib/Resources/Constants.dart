import 'package:flutter/material.dart';

const Color COLOR_APPBAR_BACKGROUND = Colors.black;
const Color COLOR_APPBAR_CONTENTS = Colors.white;
const Color COLOR_BACKGROUND = Color(0xffF5F4F9);

final BoxShadow customBoxShadow = BoxShadow(
  color: Colors.black.withOpacity(0.2),
  spreadRadius: 1,
  blurRadius: 5,
  offset: Offset(0, 3), // changes position of shadow
);
